# Scripts

This directory holds legacy or exploratory helper scripts that are useful for local research workflows but are not part of the TAPIR public package API.

Synthetic-data generation scripts have been removed from the supported workflow.

When possible, prefer the package CLI:

```bash
tapir run --config ../configs/example_parameter_setup.yaml
```
